# Database

Place your `schema.sql` and `data.sql` files here before running `setup.sh`.

```
database/
├── schema.sql   ← database schema
└── data.sql     ← seed data
```

Once both files are present, run:

```bash
bash setup.sh
```
